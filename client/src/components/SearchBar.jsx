import React from 'react';

export default function SearchBar(){
    return (
        <div>
            <input type="text">Enter a game</input>
            <button>Search</button>
        </div>
    )
}